---
name: e2e-test-run
disable-model-invocation: true
description: Run end-to-end tests validating complete user flows across the full stack. Use when user says "run e2e tests", "end to end", "Playwright", "Cypress", "test the full flow", or validating a feature works from UI to database.
---

Validate complete user flows and end-to-end functionality using specialized browsers/tools.

Steps:
1. Run browser-based tests (e.g., Playwright, Cypress)
2. Execute common flows like "Login -> Create Resource -> Logout"
3. Check for failures, timeouts, and unexpected errors

Goal: Ensure high-level features work correctly across the entire stack.
